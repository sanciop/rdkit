spack:
  specs:
  - tutorial-rdkit

  config:
    install_tree:
      root: /opt/spack
      padded_length: 128

  packages:
    all:
      require: target=x86_64_v3

  mirrors:
    local-buildcache:
      url: oci://ghcr.io/sanciop/spack-buildcache
      signed: false
